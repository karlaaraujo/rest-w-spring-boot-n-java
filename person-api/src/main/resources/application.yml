spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restful_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      # Persiste entidades definidas com JPA
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show_sql: false
